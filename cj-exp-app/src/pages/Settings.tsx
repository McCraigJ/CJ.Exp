import { Typography } from '@material-ui/core';
import * as React from 'react';

const Settings: React.SFC = () => (
<div>        
  <Typography variant="headline">Welcome Settings</Typography>
</div>
);

export default Settings;